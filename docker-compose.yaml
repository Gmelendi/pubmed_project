version: "3"

services:
  
  elasticsearch:
    
    image: "elasticsearch:7.9.2"
    ports:
      - 9200:9200
    environment:
      - discovery.type=single-node
    volumes:
      - elasticdb:/myvol

volumes:
  elasticdb: